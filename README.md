# Robin's Dotfiles

This repository contains all my dotfiles and shell scripts to install them.

## Usage

### Sway

To install sway, its dependencies and the corresponding dotfiles run
`install_sway.sh` inside of this repo:

```bash
./install_sway.sh
```
